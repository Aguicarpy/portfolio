import { createGlobalStyle } from 'styled-components';

const GlobalStyles = createGlobalStyle`
    body {
        margin: 0;
        font-family: 'Arial', sans-serif;
        background-color: #f4f4f4; /* Un tono gris claro */
        color: #333;
    }
`;

export default GlobalStyles;